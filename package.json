{
  "name": "@alphax/dynamodb",
  "version": "1.3.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wwalpha/dynamodb-helper.git"
  },
  "author": "wwalpha <wwalpha@gmail.com>",
  "license": "MIT",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "tsc -p tsconfig.build.json",
    "pretest": "docker rm -f dynamodb-helper-local >/dev/null 2>&1 || true && docker run -d --name dynamodb-helper-local -p 8000:8000 amazon/dynamodb-local -jar DynamoDBLocal.jar -inMemory -sharedDb",
    "test": "jest --runInBand",
    "posttest": "docker rm -f dynamodb-helper-local >/dev/null 2>&1 || true"
  },
  "devDependencies": {
    "@aws-sdk/types": "3.973.6",
    "@types/jest": "30.0.0",
    "@types/lodash": "4.17.24",
    "@types/node": "25.5.0",
    "jest": "30.3.0",
    "prettier": "3.8.1",
    "ts-jest": "29.4.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "3.1013.0",
    "@aws-sdk/lib-dynamodb": "3.1013.0",
    "@aws-sdk/util-dynamodb": "3.996.2",
    "lodash": "4.17.23",
    "winston": "3.19.0"
  },
  "description": "```\r yarn add https://github.com/wwalpha/dynamodb-helper.git\r or\r npm i https://github.com/wwalpha/dynamodb-helper.git\r ```",
  "bugs": {
    "url": "https://github.com/wwalpha/dynamodb-helper/issues"
  },
  "homepage": "https://github.com/wwalpha/dynamodb-helper#readme",
  "directories": {
    "test": "test"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}
